\documentclass[12pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{cite}
\usepackage[hidelinks]{hyperref}
\usepackage{geometry}
\usepackage{caption}
\captionsetup{font=footnotesize}
\geometry{a4paper, margin=2.5cm}

\title{Dinámica de Tres Niveles}
\author{Mateo Rubio Hernández}
\date{\today}

\begin{document}

\maketitle

Boyd establece que la respuesta atómica esta dada por las siguientes ecuaciones:

\begin{equation}
\dot{\rho}_{ll} = -\sum_{\substack{i \\ (W_i < W_l)}} \gamma_{li}\rho_{ll} + \sum_{\substack{i \\ (W_i > W_l)}} \gamma_{il}\rho_{ii} + \frac{i}{\hbar} \sum_{i} (\mu_{li}\rho_{il} - \mu_{il}\rho_{li})E(t) 
\end{equation}

\begin{equation}
\dot{\rho}_{lm} = -(i\omega_{lm} + \Gamma_{lm})\rho_{lm} + \frac{i}{\hbar} \sum_{i} (\mu_{li}\rho_{im} - \mu_{im}\rho_{li})E(t) \quad (l \neq m) 
\end{equation}

Donde $W_i$ es el nivel de energía del estado $i$. Luego los elementos de la matriz de densidad se pueden expandir en términos perturbativos de la siguiente forma:

\begin{equation}
\rho_{lm} = \rho_{lm}^{(0)} + \rho_{lm}^{(1)} + \rho_{lm}^{(2)} + \rho_{lm}^{(3)} + \cdots 
\end{equation}

Con condiciones iniciales que $\rho_{aa}^{(0)} = 1$ y $\rho_{lm}^{(0)} = 0$ para cualquier $l \neq m$ y para $l = m \neq a$. De aquí Boyd menciona que las contribuciones que dejará son aquellas que se mantengan en la aproximación de la onda rotante.

\section{Primer Orden Perturbativo}
Para el primer orden perturbativo $\rho_{ba}^{(1)}$:
\begin{equation}
\dot{\rho}_{ba}^{(1)} = -(i\omega_{ba} + \Gamma_{ba})\rho_{ba}^{(1)} + \frac{i}{\hbar} \sum_{i} (\mu_{bi}\rho_{ia}^{(0)} - \mu_{ia}\rho_{bi}^{(0)})E(t)
\end{equation}

Aplicando las condiciones iniciales:
\begin{equation}
\dot{\rho}_{ba}^{(1)} = -(i\omega_{ba} + \Gamma_{ba})\rho_{ba}^{(1)} + \frac{i}{\hbar} \mu_{ba} E(t)
\end{equation}

Tambien se asume que el campo incidente en el vapor atómico se puede representar como: 
\begin{equation}
E(t) = \tilde{E}_1 e^{-i\omega_1 t} + \tilde{E}_2 e^{-i\omega_2 t} + \tilde{E}_3 e^{-i\omega_3 t}
\end{equation}

Se propone como solución de estado estacionario un \textit{ansatz} que oscile a las frecuencias de las perturbaciones externas:
\begin{equation}
\rho_{ba}^{(1)} = A_1 e^{-i\omega_1 t} + A_2 e^{-i\omega_2 t} + A_3 e^{-i\omega_3 t}
\end{equation}

Al tomar la derivada temporal del \textit{ansatz}, sustituirlo junto con $E(t)$ en la ecuación diferencial simplificada, y factorizar agrupando los términos que oscilan a la misma frecuencia $e^{-i\omega_j t}$, el problema se convierte en un sistema de ecuaciones algebraicas independientes para determinar cada coeficiente $A_j$.

Resolviendo para la componente impulsada por la frecuencia $\omega_1$:
\begin{equation}
-i\omega_1 A_1 e^{-i\omega_1 t} = -(i\omega_{ba} + \Gamma_{ba}) A_1 e^{-i\omega_1 t} + \frac{i}{\hbar}\mu_{ba}\tilde{E}_1 e^{-i\omega_1 t}
\end{equation}
\begin{equation}
i(\omega_{ba} - \omega_1 - i\Gamma_{ba})A_1 = \frac{i}{\hbar}\mu_{ba}\tilde{E}_1
\end{equation}

Introduciendo la definición de desintonía de un fotón, donde $\Delta_1 = \omega_{ba} - \omega_1$, se despeja la amplitud $A_1$:
\begin{equation}
A_1 = \frac{\mu_{ba}}{\hbar} \frac{\tilde{E}_1}{\Delta_1 - i\Gamma_{ba}}
\end{equation}

Aplicando el mismo álgebra para la frecuencia $\omega_3$ con $\Delta_3 = \omega_{ba} - \omega_3$:
\begin{equation}
A_3 = \frac{\mu_{ba}}{\hbar} \frac{\tilde{E}_3}{\Delta_3 - i\Gamma_{ba}}
\end{equation}

Para el campo de frecuencia $\omega_2$, se impone la condición de conservación de energía: $2\omega_1 = \omega_2 + \omega_3 \xrightarrow{} \omega_2 = 2\omega_1 - \omega_3$, tal que en el denominador de $A_2$ se obtiene $\omega_{ba} - \omega_2 = \omega_{ba} - (2\omega_1 - \omega_3)$. 

Este resultado se puede escribir en función de las desintonías $\Delta_1$ y $\Delta_3$:
\begin{equation}
\omega_{ba} - 2\omega_1 + \omega_3 = 2\omega_{ba} - 2\omega_1 - \omega_{ba} + \omega_3 = 2(\omega_{ba} - \omega_1) - (\omega_{ba} - \omega_3) = 2\Delta_1 - \Delta_3
\end{equation}

Por ende, $A_2$ resulta en:
\begin{equation}
A_2 = \frac{\mu_{ba}}{\hbar} \frac{\tilde{E}_2}{2\Delta_1 - \Delta_3 - i\Gamma_{ba}}
\end{equation}

Sustituyendo en el \textit{ansatz} original:
\begin{equation}
\rho_{ba}^{(1)} = \frac{\mu_{ba}}{\hbar} \left[ \frac{\tilde{E}_1 e^{-i\omega_1 t}}{\Delta_1 - i\Gamma_{ba}} + \frac{\tilde{E}_2 e^{-i\omega_2 t}}{2\Delta_1 - \Delta_3 - i\Gamma_{ba}} + \frac{\tilde{E}_3 e^{-i\omega_3 t}}{\Delta_3 - i\Gamma_{ba}} \right]
\end{equation}
 
\section{Segundo Orden Perturbativo}

Para la coherencia de segundo orden $\rho_{ca}^{(2)}$:
\begin{equation}
\dot{\rho}_{ca}^{(2)} = -(i\omega_{ca} + \Gamma_{ca})\rho_{ca}^{(2)} + \frac{i}{\hbar} \sum_{i} (\mu_{ci}\rho_{ia}^{(1)} - \mu_{ia}\rho_{ci}^{(1)})E(t)
\end{equation}

Como toda la población inicial partió del estado fundamental $a$, el único elemento de coherencia de primer orden no nulo es $\rho_{ba}^{(1)}$, reduciendo la ecuación a:
\begin{equation}
\dot{\rho}_{ca}^{(2)} = -(i\omega_{ca} + \Gamma_{ca})\rho_{ca}^{(2)} + \frac{i}{\hbar} \mu_{cb} \rho_{ba}^{(1)} E(t)
\end{equation}

Al multiplicar $\rho_{ba}^{(1)}$ por $E(t)$, se generan nueve términos cruzados. Sin embargo solo se conservan aquellos términos que contribuyen a la respuesta de dos fotones resonante en el átomo:

1. El producto del término impulsado por $\omega_1$ en $\rho_{ba}^{(1)}$ interactuando nuevamente con el campo $\omega_1$:
\begin{equation}
\left( \frac{\tilde{E}_1 e^{-i\omega_1 t}}{\Delta_1 - i\Gamma_{ba}} \right) \tilde{E}_1 e^{-i\omega_1 t} = \frac{\tilde{E}_1^2 e^{-i2\omega_1 t}}{\Delta_1 - i\Gamma_{ba}}
\end{equation}

2. El producto del término impulsado por $\omega_2$ en $\rho_{ba}^{(1)}$ interactuando con el campo $\omega_3$:
\begin{equation}
\left( \frac{\tilde{E}_2 e^{-i\omega_2 t}}{2\Delta_1 - \Delta_3 - i\Gamma_{ba}} \right) \tilde{E}_3 e^{-i\omega_3 t} = \frac{\tilde{E}_2 \tilde{E}_3 e^{-i(\omega_2+\omega_3)t}}{2\Delta_1 - \Delta_3 - i\Gamma_{ba}}
\end{equation}

3. El producto del término impulsado por $\omega_3$ en $\rho_{ba}^{(1)}$ interactuando con el campo $\omega_2$:
\begin{equation}
\left( \frac{\tilde{E}_3 e^{-i\omega_3 t}}{\Delta_3 - i\Gamma_{ba}} \right) \tilde{E}_2 e^{-i\omega_2 t} = \frac{\tilde{E}_2 \tilde{E}_3 e^{-i(\omega_2+\omega_3)t}}{\Delta_3 - i\Gamma_{ba}}
\end{equation}

De este modo, se propone un \textit{ansatz} para $\rho_{ca}^{(2)}$ que oscile a estas mismas frecuencias:
\begin{equation}
\rho_{ca}^{(2)} = B_1 e^{-i2\omega_1 t} + B_{23} e^{-i(\omega_2+\omega_3)t}
\end{equation}

Para los términos de la frecuencia $2\omega_1$:
\begin{align}
-i2\omega_1 B_1 e^{-i2\omega_1 t} = -(i\omega_{ca} + \Gamma_{ca}) B_1 e^{-i2\omega_1 t} + \frac{i}{\hbar} \mu_{cb} \frac{\mu_{ba}}{\hbar} \frac{\tilde{E}_1^2 e^{-i2\omega_1 t}}{\Delta_1 - i\Gamma_{ba}}\\
i(\omega_{ca} - 2\omega_1 - i\Gamma_{ca}) B_1 = \frac{i}{\hbar^2} \mu_{cb} \mu_{ba} \frac{\tilde{E}_1^2}{\Delta_1 - i\Gamma_{ba}}
\end{align}


Definiendo la desintonía de dos fotones dada por $\Delta_2 = \omega_{ca} - 2\omega_1$, se tiene que: 
\begin{equation}
B_1 = \frac{\mu_{cb} \mu_{ba}}{\hbar^2} \frac{\tilde{E}_1^2}{(\Delta_2 - i\Gamma_{ca})(\Delta_1 - i\Gamma_{ba})}
\end{equation}

Mientras que para los términos de la frecuencia $\omega_2 + \omega_3$: 
\begin{align}
    -i(\omega_2  + \omega_3)B_{23} = -(i\omega_{ca} + \Gamma_{ca})B_{23} + \frac{i}{\hbar^2}\mu_{cb}\mu_{ba}\left(\frac{\tilde{E}_2 \tilde{E}_3}{\Delta_3 - i\Gamma_{ba}} + \frac{\tilde{E}_2 \tilde{E}_3}{2\Delta_1 - \Delta_3 - i\Gamma_{ba}}\right)\\
    i(\omega_{ca} - \omega_2  - \omega_3 - i\Gamma_{ca})B_{23} = \frac{i}{\hbar^2}\mu_{cb}\mu_{ba}\left(\frac{\tilde{E}_2 \tilde{E}_3}{\Delta_3 - i\Gamma_{ba}} + \frac{\tilde{E}_2 \tilde{E}_3}{2\Delta_1 - \Delta_3 - i\Gamma_{ba}}\right)
\end{align}
Aplicando conservación de energía se tiene que $\Delta_2= \omega_{ca} - (\omega_2 + \omega_3)$ y por tanto:
\begin{equation}
B_{23} = \frac{\mu_{cb} \mu_{ba}}{\hbar^2} \left[ \frac{\tilde{E}_2 \tilde{E}_3}{(\Delta_2 - i\Gamma_{ca})(2\Delta_1 - \Delta_3 - i\Gamma_{ba})} + \frac{\tilde{E}_2 \tilde{E}_3}{(\Delta_2 - i\Gamma_{ca})(\Delta_3 - i\Gamma_{ba})} \right]
\end{equation}

Reemplazando en el \textit{ansatz}:

\begin{equation}
\begin{aligned}
\rho_{ca}^{(2)} &= \frac{\mu_{cb}\mu_{ba}}{\hbar^2} \left[ \frac{\tilde{E}_1^2 e^{-i2\omega_1 t}}{(\Delta_1 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})} \right. + \frac{\tilde{E}_2\tilde{E}_3 e^{-i(\omega_2+\omega_3)t}}{(2\Delta_1 - \Delta_3 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})} \\
&\quad \left. + \frac{\tilde{E}_2\tilde{E}_3 e^{-i(\omega_2+\omega_3)t}}{(\Delta_3 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})} \right]
\end{aligned} 
\end{equation}

\section{Tercer Orden Perturbativo}

La derivación de la coherencia de tercer orden $\rho_{cb}^{(3)}$ se obtiene al utilizar la coherencia de segundo orden $\rho_{ca}^{(2)}$ calculada previamente como el término fuente que impulsa esta nueva perturbación. Este paso describe físicamente una transición descendente estimulada desde el nivel superior $c$ hacia el estado intermedio $b$.

Se parte de la ecuación de evolución para el elemento de la matriz de densidad correspondiente a la transición $c \to b$:
\begin{equation}
\dot{\rho}_{cb}^{(3)} = -(i\omega_{cb} + \Gamma_{cb})\rho_{cb}^{(3)} + \frac{i}{\hbar} \sum_i (\mu_{ci}\rho_{ib}^{(2)} - \mu_{ib}\rho_{ci}^{(2)})E(t)
\end{equation}

Para evaluar el conmutador de la interacción, se debe analizar qué elementos de segundo orden no son nulos. El único camino cuántico poblado en el segundo orden de perturbación que conecta estos estados es la coherencia $\rho_{ca}^{(2)}$. Por lo tanto, en la sumatoria el único índice que sobrevive es $i=a$.

Dado que la transición directa de dos fotones entre los estados $c$ y $a$ está prohibida por las reglas de selección dipolares ($\mu_{ca} = 0$), el primer término del paréntesis se anula. El término restante es $-\mu_{ab}\rho_{ca}^{(2)}$. Expresando el momento dipolar conjugado como $\mu_{ab} = \mu_{ba}^*$, la ecuación diferencial se simplifica a la interacción directa con el campo externo:
\begin{equation}
\dot{\rho}_{cb}^{(3)} = -(i\omega_{cb} + \Gamma_{cb})\rho_{cb}^{(3)} - \frac{i}{\hbar} \mu_{ba}^* \rho_{ca}^{(2)} E(t)
\end{equation}



Al representar una transición descendente (emisión estimulada acoplada a la polarización), el sistema extrae energía del campo macroscópico oscilando en la dirección opuesta a la absorción. Matemáticamente, bajo la Aproximación de la Onda Rotante, esto significa que la coherencia interactúa con las componentes de frecuencia negativa (complejas conjugadas) del campo electromagnético total:
\begin{equation}
E^*(t) = \tilde{E}_1^* e^{i\omega_1 t} + \tilde{E}_2^* e^{i\omega_2 t} + \tilde{E}_3^* e^{i\omega_3 t}
\end{equation}

La coherencia de segundo orden $\rho_{ca}^{(2)}$ posee un coeficiente global $\frac{\mu_{cb}\mu_{ba}}{\hbar^2}$. Al multiplicar este coeficiente por el término $-\frac{i}{\hbar}\mu_{ba}^*$ de la ecuación diferencial, se obtiene de forma exacta el factor común externo de la ecuación final:
\begin{equation}
-\frac{i}{\hbar} \mu_{ba}^* \left( \frac{\mu_{cb}\mu_{ba}}{\hbar^2} \right) = -\frac{i}{\hbar^3} |\mu_{ba}|^2 \mu_{cb}
\end{equation}

El núcleo de la derivación radica en realizar el producto cruzado entre los $3$ términos contenidos en los corchetes de $\rho_{ca}^{(2)}$ y las $3$ componentes del campo conjugado $E^*(t)$. Este producto genera $3 \times 3 = 9$ términos impulsores individuales, lo que explica la estructura expandida de la ecuación (4a).

A modo de demostración algebraica, se evalúa el primer término de $\rho_{ca}^{(2)}$, el cual es proporcional a $\tilde{E}_1^2 e^{-i2\omega_1 t}$, al interactuar con el primer componente del campo $\tilde{E}_1^* e^{i\omega_1 t}$:
\begin{equation}
\left( \frac{\tilde{E}_1^2 e^{-i2\omega_1 t}}{(\Delta_1 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})} \right) \tilde{E}_1^* e^{i\omega_1 t} = \frac{|\tilde{E}_1|^2 \tilde{E}_1 e^{-i\omega_1 t}}{(\Delta_1 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})}
\end{equation}

Este término oscila a una frecuencia neta de $\omega_1$. Se propone entonces un \textit{ansatz} de la forma $C_1 e^{-i\omega_1 t}$ para esta fracción de $\rho_{cb}^{(3)}$. Al sustituirlo en la ecuación diferencial y agrupar:
\begin{equation}
-i\omega_1 C_1 e^{-i\omega_1 t} = -(i\omega_{cb} + \Gamma_{cb}) C_1 e^{-i\omega_1 t} - \frac{i}{\hbar^3} |\mu_{ba}|^2 \mu_{cb} \frac{|\tilde{E}_1|^2 \tilde{E}_1 e^{-i\omega_1 t}}{(\Delta_1 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})}
\end{equation}
\begin{equation}
i(\omega_{cb} - \omega_1 - i\Gamma_{cb}) C_1 = - \frac{i}{\hbar^3} |\mu_{ba}|^2 \mu_{cb} \frac{|\tilde{E}_1|^2 \tilde{E}_1}{(\Delta_1 - i\Gamma_{ba})(\Delta_2 - i\Gamma_{ca})}
\end{equation}

Para alinear el denominador con las variables de desintonía del sistema, se expresa la resta de frecuencias $\omega_{cb} - \omega_1$ en función de $\Delta_1$ y $\Delta_2$. Sabiendo que $\omega_{cb} = \omega_{ca} - \omega_{ba}$:
\begin{equation}
\omega_{cb} - \omega_1 = \omega_{ca} - \omega_{ba} - \omega_1
\end{equation}
Sumando y restando $\omega_1$:
\begin{equation}
\omega_{ca} - 2\omega_1 - \omega_{ba} + \omega_1 = (\omega_{ca} - 2\omega_1) - (\omega_{ba} - \omega_1)
\end{equation}
Insertando las definiciones $\Delta_2 = \omega_{ca} - 2\omega_1$ y $\Delta_1 = \omega_{ba} - \omega_1$, se demuestra que el término de resonancia efectivo es:
\begin{equation}
\omega_{cb} - \omega_1 = \Delta_2 - \Delta_1
\end{equation}
Lo que genera el nuevo denominador $(\Delta_2 - \Delta_1 - i\Gamma_{cb})$ en el primer término de la ecuación.



Al multiplicar este mismo primer término de $\rho_{ca}^{(2)}$ por la segunda componente del campo conjugado, $\tilde{E}_2^* e^{i\omega_2 t}$, la frecuencia de oscilación resultante es $2\omega_1 - \omega_2$. Por conservación de energía en el mezclado de cuatro ondas ($2\omega_1 = \omega_2 + \omega_3$), esta frecuencia se reduce exactamente a $\omega_3$, generando el término numerador $\tilde{E}_1^2 \tilde{E}_2^* e^{-i\omega_3 t}$.

El denominador de frecuencia asociado se resuelve aislando $\omega_{cb} - \omega_3$:
\begin{equation}
\omega_{cb} - \omega_3 = \omega_{ca} - \omega_{ba} - \omega_3
\end{equation}
Para expresarlo estrictamente en $\Delta_1, \Delta_2, \Delta_3$, se construye la combinación $\Delta_2 + \Delta_3 - 2\Delta_1$:
\begin{equation}
(\omega_{ca} - 2\omega_1) + (\omega_{ba} - \omega_3) - 2(\omega_{ba} - \omega_1) = \omega_{ca} - \omega_{ba} - \omega_3
\end{equation}
Confirmando matemáticamente la estructura del segundo denominador de la ecuación general $(\Delta_2 + \Delta_3 - 2\Delta_1 - i\Gamma_{cb})$.

El procedimiento de factor constante extraído como $\frac{1}{\Delta_2 - i\Gamma_{ca}}$, combinado con la resolución algebraica de las $9$ diferencias de fase para cada producto cruzado de los campos paramétricos y la coherencia de dos fotones, conforma la totalidad de la expresión de la matriz de densidad para $\rho_{cb}^{(3)}$.

\end{document}